# ============================================================
# LiveMapEvents — Server Configuration
# ============================================================
# FILL IN YOUR VALUES BELOW, then run:
#   cp server.conf.example server.conf
#   nano server.conf           (edit with your real values)
#   ./deploy-remote.sh         (deploy from your Mac)
# ============================================================

# ── Your Server ──
SERVER_IP=187.124.180.7                     # <-- Your server IP (e.g. 165.22.48.100)
SERVER_HOSTNAME=live-events-map.tech       # <-- Your domain, or same as SERVER_IP if no domain
SERVER_USER=deploy                         # <-- SSH user on the server
SERVER_SSH_KEY="/c/Users/livemap_deploy"
SERVER_DEPLOY_PATH=/opt/livemap            # <-- Where the app lives on the server

# ── App Settings ──
APP_PORT=80                              # <-- Port the backend API listens on
FRONTEND_PORT=3000                         # <-- Port the Flutter web frontend listens on
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:jz7kEqiYQPugOjPkjD0AJ0qoBPQ8jB+EtWlawiWeKZc=

# ── Database ──
DB_HOST=live-events-map-live-events-map.e.aivencloud.com       # <-- from Aiven console (hostname only, not the full URL)
DB_PORT=16462
DB_DATABASE=defaultdb
DB_USERNAME=avnadmin
DB_PASSWORD=AVNS_0ZPY7JFYEMFNjcJOD2B
DB_SSLMODE=require

# ── Google OAuth ──
GOOGLE_CLIENT_ID=103431306679-m3dg292f9e0a71oj4ed1ts9nljsrbt7r.apps.googleusercontent.com

# ── UltraMsg (WhatsApp OTP) ──
ULTRAMSG_URL=https://api.ultramsg.com
ULTRAMSG_INSTANCE_ID=your-instance-id      # <-- From ultramsg.com dashboard
ULTRAMSG_TOKEN=your-token                  # <-- From ultramsg.com dashboard