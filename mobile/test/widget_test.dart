// Smoke test placeholder. The previous default counter test was deleted
// because the app does not contain a counter widget. Replace this with real
// widget tests as features stabilise.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('arithmetic smoke test', () {
    expect(1 + 1, equals(2));
  });
}
